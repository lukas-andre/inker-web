---
// Home.astro
---

<section class="min-h-screen bg-background px-4 md:px-8 py-12 md:py-24 relative overflow-hidden">

  {/* Fondo decorativo */}
  <div class="absolute inset-0 overflow-hidden">
    <div class="absolute inset-0 bg-[url('/img/grid-pattern.png')] opacity-5"></div>
  </div>

  <div class="max-w-7xl mx-auto relative">
    <div class="grid md:grid-cols-2 gap-12 items-center">

      {/* Contenido izquierdo */}
      <div class="space-y-8">
        <div class="space-y-4">
          <h1 class="text-4xl md:text-6xl font-nexa-bold leading-tight text-white">
            Encuentra a tu{" "}
            <span class="text-accent">tatuador perfecto</span>
          </h1>
          <p class="text-neutral text-lg md:text-xl font-nexa-light text-white">
            Conecta con tatuadores de todo el mundo y descubre estilos únicos que se adaptan a tu personalidad.
          </p>
        </div>

        <a
          href="#download"
          class="inline-block bg-accent hover:bg-accent-hover text-white font-nexa-bold px-8 py-3 rounded-full transition-all duration-300 transform hover:scale-105"
        >
          Descargar la app
        </a>

        <section>
          {/* Espacio para imágenes de tatuajes y mockups */}
          <div class="grid grid-cols-2 gap-4 mt-12">
            {/* Imagen tatuaje 1 */}
            <div class="aspect-[3/4] rounded-2xl overflow-hidden bg-neutral/10">
              <img src="/public/img/2.png" alt="Tatuaje 1" class="w-full h-full object-cover scale-90" />
            </div>
            {/* Imagen tatuaje 2 */}
            <div class="aspect-[3/4] rounded-2xl overflow-hidden bg-neutral/10">
              <img src="/public/img/1.png" alt="Tatuaje 2" class="w-full h-full object-cover scale-90" />
            </div>
            {/* Imagen mockup 1 */}
            <div class="aspect-[3/4] rounded-2xl overflow-hidden bg-neutral/10 col-span-2 md:col-span-1">
              <img src="/public/img/3.png" alt="Mockup 1" class="w-full h-full object-cover" />
            </div>
            {/* Imagen mockup 2 */}
            <div class="aspect-[3/4] rounded-2xl overflow-hidden bg-neutral/10 col-span-2 md:col-span-1">
              <img src="/public/img/4.png" alt="Mockup 2" class="w-full h-full object-cover" />
            </div>
          </div>
        
          {/* Features */}
          <div class="absolute bottom-0 left-0 space-y-4 mt-4">
            <div class="flex items-center space-x-3 text-white">
              <svg class="w-5 h-5 text-accent" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="font-nexa text-white">Perfiles verificados</span>
            </div>
            <div class="flex items-center space-x-3 text-white">
              <svg class="w-5 h-5 text-accent" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zm3.707-9.293a1 1 0 00-1.414-1.414L9 10.586 7.707 9.293a1 1 0 00-1.414 1.414l2 2a1 1 0 001.414 0l4-4z" clip-rule="evenodd" />
              </svg>
              <span class="font-nexa text-white">Reseñas y calificaciones</span>
            </div>
          </div>
        </section>

<head>
  <title>Encuentra a tu Tatuador Perfecto</title>
  <meta name="description" content="Conecta con tatuadores de todo el mundo y descubre estilos únicos que se adaptan a tu personalidad.">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta charset="UTF-8">
</head>

<style>
  /* Variables de color */
  :root {
    --color-background: #0a0612;
    --color-accent: #8B5CF6;
    --color-accent-hover: #7C3AED;
    --color-neutral: #94A3B8;
  }

  /* Importar fuente Nexa */
  @font-face {
    font-family: 'Nexa-Light';
    src: url('/fonts/Nexa-Light.woff2') format('woff2'),
         url('/fonts/Nexa-Light.woff') format('woff');
    font-weight: 300;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: 'Nexa';
    src: url('/fonts/Nexa-Regular.woff2') format('woff2'),
         url('/fonts/Nexa-Regular.woff') format('woff');
    font-weight: 400;
    font-style: normal;
    font-display: swap;
  }

  @font-face {
    font-family: 'Nexa-Bold';
    src: url('/fonts/Nexa-Bold.woff2') format('woff2'),
         url('/fonts/Nexa-Bold.woff') format('woff');
    font-weight: 700;
    font-style: normal;
    font-display: swap;
  }

  /* Estilos generales */
  section {
    background-color: var(--color-background);
    font-family: 'Nexa', sans-serif;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .grid-cols-2 {
      grid-template-columns: 1fr;
    }
  }
</style>