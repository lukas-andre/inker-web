---
// Astro component
---

<section
  class="relative min-h-screen bg-[#0a0a0a] overflow-hidden px-4 py-12 md:px-6 lg:px-8"
>
  {/* Background pattern */}
  <div class="absolute inset-0 opacity-10">
    <div
      class="absolute inset-0 bg-[linear-gradient(45deg,transparent_25%,#6d28d9_25%,#6d28d9_50%,transparent_50%,transparent_75%,#6d28d9_75%)]"
      style="background-size: 60px 60px"
    >
    </div>
  </div>

  <div class="relative max-w-7xl mx-auto">
    {/* Top pill */}
    <div class="absolute top-4 right-4 md:right-8">
      <div class="bg-purple-600 text-white px-6 py-2 rounded-full text-sm">
        Tu tatuaje, tu artista, tu estilo.
      </div>
    </div>

    <div class="grid lg:grid-cols-2 gap-12 items-center pt-16">
      {/* Left column */}
      <div class="space-y-8">
        <div class="space-y-4">
          <h1
            class="text-4xl md:text-5xl lg:text-6xl font-bold text-white leading-tight"
          >
            Encuentra a tu <span class="text-purple-500">tatuador perfecto</span
            >
          </h1>
          <p class="text-gray-400 text-lg md:text-xl max-w-xl">
            Conecta con tatuadores de todo el mundo y descubre estilos únicos
            que se adapten a tu personalidad.
          </p>
        </div>

        <button
          class="bg-purple-600 hover:bg-purple-700 text-white px-8 py-3 rounded-full text-lg font-medium transition-colors"
        >
          Descargar la app
        </button>

        <div class="relative h-80 lg:h-96 mt-8 lg:mt-12">
          <img
            src="/placeholder.svg?height=400&width=600"
            alt="Artistas del tatuaje"
            class="absolute inset-0 w-full h-full object-cover rounded-2xl"
          />
        </div>
      </div>

      {/* Right column */}
      <div class="relative">
        {/* Phone mockups */}
        <div class="relative h-[600px]">
          <div class="absolute right-0 top-0 w-72 transform rotate-6">
            <img
              src="/placeholder.svg?height=700&width=350"
              alt="App screenshot 1"
              class="w-full h-auto rounded-3xl shadow-2xl"
            />
          </div>
          <div class="absolute right-20 top-20 w-72 transform -rotate-3">
            <img
              src="/placeholder.svg?height=700&width=350"
              alt="App screenshot 2"
              class="w-full h-auto rounded-3xl shadow-2xl"
            />
          </div>
        </div>

        {/* Feature list */}
        <div class="space-y-4 mt-8">
          <div class="flex items-center gap-3 text-white">
            <div class="bg-green-500/20 p-1 rounded-full">
              <svg
                class="w-5 h-5 text-green-500"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-lg">Perfiles verificados</span>
          </div>
          <div class="flex items-center gap-3 text-white">
            <div class="bg-green-500/20 p-1 rounded-full">
              <svg
                class="w-5 h-5 text-green-500"
                fill="none"
                stroke="currentColor"
                viewBox="0 0 24 24"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M5 13l4 4L19 7"></path>
              </svg>
            </div>
            <span class="text-lg">Reseñas y calificaciones</span>
          </div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  /* Add any additional Astro-specific styles here */
  section {
    background-image: radial-gradient(
      circle at center,
      #1a1a1a 0%,
      #0a0a0a 100%
    );
  }
</style>
