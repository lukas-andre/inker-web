---
// src/components/sections/nosotros.astro
---

<section class="about-section">
  <div class="container">
    <div class="content-wrapper">
      <!-- Columna izquierda -->
      <div class="info-column">
        <h1 class="title">
          <span>Sobre nosotros</span>
        </h1>

        <div class="text-content">
          <div class="content-block">
            <p>
              Nuestra app te simplifica la búsqueda de tu próximo tatuaje. Con 
              unos pocos clics, podrás encontrar artistas talentosos en tu zona
              o en cualquier parte del mundo. Personaliza tu búsqueda por 
              estilo, temática y presupuesto. ¡Tu tatuaje ideal está a solo un
              toque de distancia!
            </p>
            <p>
              Únete a una comunidad global de amantes del tatuaje. Inspírate con
              miles de diseños y descubre nuevos artistas. Nuestra app es más
              que una simple herramienta de búsqueda: es tu fuente de 
              inspiración para tu próximo tatuaje.
            </p>
          </div>
        </div>

        <a href="#" class="cta-button">
          ¡Encuentra tu artista!
          <span class="button-glow"></span>
        </a>
      </div>

      <!-- Columna derecha -->
      <div class="visual-column">
        <div class="phone-wrapper">
          <img
            src="/img/nosotros/image 14.png"
            alt="App interface"
            class="phone-mockup"
          />
          <div class="phone-glow"></div>
        </div>

        <img
          src="/img/nosotros/Frame 41.png"
          alt="Tattoo artist"
          class="floating-image artist"
        />
      </div>
    </div>

    <!-- Imágenes decorativas -->
    <img
      src="/img/nosotros/Mask group.png"
      alt=""
      class="decor-image bottom"
    />
    <img
      src="/img/nosotros/Frame 36.png"
      alt=""
      class="decor-image top"
    />
  </div>
</section>

<style>
  .about-section {
    background-color: #0A031F;
    position: relative;
    padding: 7rem 0;
    overflow: hidden;
  }

  .container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 1rem;
    position: relative;
  }

  .content-wrapper {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 3rem;
    position: relative;
    z-index: 2;
  }

  .info-column {
    display: flex;
    flex-direction: column;
    gap: 2rem;
  }

  .title {
    font-size: 3.5rem;
    color: #F3F4F6;
    position: relative;
    display: inline-block;
  }

  .title::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 0;
    width: 60%;
    height: 4px;
    background: linear-gradient(90deg, #4318FF, transparent);
  }

  .text-content {
    color: #686D90;
    font-size: 1.1rem;
    line-height: 1.7;
  }

  .content-block {
    position: relative;
    padding-left: 1.5rem;
    border-left: 4px solid #4318FF;
    transition: transform 0.3s ease;
  }

  .content-block:hover {
    transform: translateX(10px);
  }

  .content-block p {
    margin-bottom: 1.5rem;
  }

  .content-block::before {
    content: '';
    position: absolute;
    left: -4px;
    top: 0;
    width: 4px;
    height: 0;
    background: #7450FF;
    transition: height 0.3s ease;
  }

  .content-block:hover::before {
    height: 100%;
  }

  .cta-button {
    display: inline-block;
    background: #4318FF;
    color: white;
    padding: 1rem 2rem;
    border-radius: 9999px;
    font-weight: 600;
    position: relative;
    overflow: hidden;
    transition: transform 0.3s ease;
    box-shadow: 0 0 20px rgba(67, 24, 255, 0.3);
  }

  .button-glow {
    position: absolute;
    top: -50%;
    left: -50%;
    width: 200%;
    height: 200%;
    background: linear-gradient(45deg, transparent, rgba(255, 255, 255, 0.2), transparent);
    transform: rotate(45deg);
    animation: buttonGlow 3s infinite;
  }

  .visual-column {
    position: relative;
    display: flex;
    justify-content: center;
    align-items: center;
  }

  .phone-wrapper {
    position: relative;
    z-index: 2;
  }

  .phone-mockup {
    width: 250px;
    border-radius: 1.5rem;
    box-shadow: 0 0 30px rgba(67, 24, 255, 0.2);
    transition: transform 0.3s ease;
  }

  .phone-wrapper:hover .phone-mockup {
    transform: translateY(-10px);
  }

  .phone-glow {
    position: absolute;
    inset: -20px;
    background: radial-gradient(circle at center, rgba(67, 24, 255, 0.2), transparent);
    filter: blur(20px);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .phone-wrapper:hover .phone-glow {
    opacity: 1;
  }

  .floating-image {
    position: absolute;
    transition: transform 0.3s ease;
  }

  .floating-image.artist {
    width: 200px;
    top: -100px;
    right: 15px;
    animation: float 6s ease-in-out infinite;
  }

  .decor-image {
    position: absolute;
    width: 150px;
    opacity: 0.8;
    transition: opacity 0.3s ease;
  }

  .decor-image.bottom {
    bottom: 0;
    right: 0;
    animation: pulse 8s ease-in-out infinite;
  }

  .decor-image.top {
    top: 2.5rem;
    left: 0;
    animation: pulse 8s ease-in-out infinite reverse;
  }

  @keyframes buttonGlow {
    0% { left: -50% }
    100% { left: 150% }
  }

  @keyframes float {
    0%, 100% { transform: translateY(0); }
    50% { transform: translateY(-20px); }
  }

  @keyframes pulse {
    0%, 100% { opacity: 0.8; transform: scale(1); }
    50% { opacity: 0.6; transform: scale(0.95); }
  }

  /* Responsive */
  @media (max-width: 1024px) {
    .content-wrapper {
      grid-template-columns: 1fr;
      gap: 4rem;
    }

    .visual-column {
      order: -1;
    }

    .floating-image.artist {
      top: -50px;
      right: 50px;
    }
  }

  @media (max-width: 768px) {
    .title {
      font-size: 2.5rem;
    }

    .floating-image.artist {
      width: 150px;
      top: -30px;
      right: 30px;
    }

    .decor-image {
      width: 100px;
    }
  }
</style>