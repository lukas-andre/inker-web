---
// components/ContactFloat.astro
---

<div class="contact-float">
    <a href="mailto:lucas@inker.studio" class="contact-button">
      <svg xmlns="http://www.w3.org/2000/svg" class="icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/>
        <polyline points="22,6 12,13 2,6"/>
      </svg>
      <span class="tooltip">Contacto</span>
    </a>
  </div>
  
  <style>
    .contact-float {
      position: fixed;
      bottom: 2rem;
      right: 2rem;
      z-index: 1000;
    }
  
    .contact-button {
      display: flex;
      align-items: center;
      justify-content: center;
      width: 3.5rem;
      height: 3.5rem;
      background: #4318FF;
      border-radius: 50%;
      color: white;
      transition: all 0.3s ease;
      position: relative;
      box-shadow: 0 4px 15px rgba(67, 24, 255, 0.3);
    }
  
    .contact-button:hover {
      transform: translateY(-5px);
      box-shadow: 0 6px 20px rgba(67, 24, 255, 0.4);
    }
  
    .icon {
      width: 1.5rem;
      height: 1.5rem;
    }
  
    .tooltip {
      position: absolute;
      top: -2.5rem;
      left: 50%;
      transform: translateX(-50%);
      background: #4318FF;
      color: white;
      padding: 0.5rem 1rem;
      border-radius: 0.5rem;
      font-size: 0.875rem;
      opacity: 0;
      visibility: hidden;
      transition: all 0.3s ease;
      white-space: nowrap;
    }
  
    .tooltip::after {
      content: '';
      position: absolute;
      bottom: -0.25rem;
      left: 50%;
      transform: translateX(-50%) rotate(45deg);
      width: 0.5rem;
      height: 0.5rem;
      background: #4318FF;
    }
  
    .contact-button:hover .tooltip {
      opacity: 1;
      visibility: visible;
      top: -3rem;
    }
  
    @media (max-width: 768px) {
      .contact-float {
        bottom: 1.5rem;
        right: 1.5rem;
      }
    }
  </style>